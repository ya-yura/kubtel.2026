---
import { getPhoneHref, getSitePath, SITE } from "@config/site";
import SectionHeading from "@components/ui/SectionHeading.astro";

const chatLinks = [
  SITE.maxChatUrl ? { label: "MAX", href: SITE.maxChatUrl } : null,
  SITE.vkChatUrl ? { label: "VK", href: SITE.vkChatUrl } : null
].filter((item): item is { label: string; href: string } => Boolean(item));
---

<section class="section-pad support-pathways" id="support-pathways">
  <SectionHeading
    eyebrow="Каналы связи"
    title="Выберите удобный способ обращения"
    description="Абоненту не нужно оставлять контакты заново, если он хочет сам позвонить или написать. Основные каналы вынесены первыми."
  />
  <div class="pathway-grid support-channel-grid">
    <article>
      <span>Телефон</span>
      <h3>{SITE.supportDirectPhone}</h3>
      <p>Для аварий, вопросов по услуге, оплате и состоянию подключения.</p>
      <a href={getPhoneHref(SITE.supportDirectPhone)}>Позвонить</a>
    </article>
    <article>
      <span>Почта</span>
      <h3>{SITE.supportEmail}</h3>
      <p>Для заявок, которые удобно описать письмом: адрес, договор, скриншот, история вопроса.</p>
      <a href={`mailto:${SITE.supportEmail}`}>Написать</a>
    </article>
    {
      chatLinks.length > 0 && (
        <article>
          <span>Мессенджеры</span>
          <h3>Официальные чаты</h3>
          <p>Выберите подтверждённый канал поддержки.</p>
          <div class="button-row">
            {chatLinks.map((chat) => (
              <a href={chat.href} target="_blank" rel="noopener noreferrer">
                {chat.label}
              </a>
            ))}
          </div>
        </article>
      )
    }
  </div>
  <div class="pathway-grid support-faq-grid">
    <article>
      <span>Оплата</span>
      <h3>Баланс и платежи</h3>
      <p>Переход к оплате и реквизитам вынесен в отдельный раздел.</p>
      <a href={getSitePath("/payment/")}>Оплатить</a>
    </article>
    <article>
      <span>Пауза</span>
      <h3>Добровольная блокировка</h3>
      <p>Можно сохранить баланс, когда интернет временно не нужен.</p>
      <a href="#faq">Открыть FAQ</a>
    </article>
    <article>
      <span>Документы</span>
      <h3>Условия и реквизиты</h3>
      <p>Юридические данные, условия связи и политика персональных данных.</p>
      <a href={getSitePath("/documents/")}>Документы</a>
    </article>
  </div>
</section>
